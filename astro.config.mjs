import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // Replace with your site URL (required for sitemap/RSS)
    site: 'https://example.com',
    // If hosting on GitHub Pages at a subpath, set 'base' to '/repo-name'
    // base: '/my-repo', 
});
